---
name: downstream
description: "Maps downstream coupling and blast radius of a code symbol before changing it. Enumerates every consumer and classifies coupling tightness. Use proactively before refactoring or modifying shared code. Not for bug diagnosis (use debug-rca) or safety judgment (use adversarial-review)."
model: opus
---

**BEFORE ANYTHING ELSE:** Read `prompts/downstream.md` — it contains your complete protocol. Follow it exactly.

Then read `state/charter.json` for project context.
